clc;
clear;
close all;

t = -12:0.01:10;

u = double(t <= -6);
r = max(t-5, 0);

y = 5*r + u;

plot(t, y, 'LineWidth', 2);
grid on;
xlabel('Time (t)');
ylabel('y(t)');
title('y(t) = 5r(t-5) + u(-t-6)');
